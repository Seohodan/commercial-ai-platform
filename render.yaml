services:
  - type: web
    name: commercial-ai-platform
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    healthCheckPath: /
    healthCheckTimeout: 180
